# Legacy frontend files

Files in this folder are kept for reference only.

They are NOT loaded by the main Inertia resolver (`resources/js/app.jsx`) because it only globs `Pages/**/*.jsx`.

If you decide to restore any of these files, move them back into the appropriate folder and update imports/routes.
